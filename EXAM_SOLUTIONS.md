# 🎯 เฉลยละเอียดและเกณฑ์การให้คะแนน (Exam Solutions & Rubrics)
**วิชา Data Science ทีละก้าว (Week 01 - 05)**

---

## 📌 สรุปคีย์คำตอบด่วน (Quick Answer Key: 30 ข้อปรนัย)

| ข้อ | คำตอบ | สัปดาห์ | หัวข้อหลักและการประยุกต์ |
|:---:|:---:|:---:|:---|
| **1** | **B** | Week 01 | มิติของ `df.head(3).shape` คือ `(3, 15)` |
| **2** | **B** | Week 01 | การวิเคราะห์ `FileNotFoundError` จาก Relative Path ของ Subfolder |
| **3** | **B** | Week 01 | ความแตกต่างของ Data Type: `csv.DictReader` (str) vs `pd.read_csv` (int64) |
| **4** | **B** | Week 01 | ไวยากรณ์ `df.shape[0]` (Attribute ไม่ใส่วงเล็บ) |
| **5** | **B** | Week 01 | ชนิดข้อมูลของแถวจาก `csv.DictReader` คือ `<class 'dict'>` |
| **6** | **B** | Week 01 | มิติของ DataFrame ที่สร้างจาก Dict: `(3, 2)` |
| **7** | **A** | Week 02 | `type(df["name"])` คือ `Series` และ `df[["name"]].shape` คือ `(46, 1)` |
| **8** | **B** | Week 02 | Immutability ของ `.rename()` โดยไม่กำหนดตัวแปรรับค่า |
| **9** | **B** | Week 02 | `KeyError` จาก Case-Sensitivity (`Overall` vs `overall`) |
| **10** | **A** | Week 02 | การแปลงโค้ด Pure Python Wrangling เป็น Pandas Chain |
| **11** | **B** | Week 02 | การป้องกัน Side-effects ต่อ `df` เดิมด้วย `.copy()` |
| **12** | **A** | Week 02 | `df.columns` คือ Index และ `.tolist()` แปลงเป็น list |
| **13** | **B** | Week 03 | การนับจุดสูญหายจริงในไฟล์ `footballers.csv`: `1 1` |
| **14** | **A** | Week 03 | การคำนวณเปรียบเทียบผลกระทบของ 0 (18.4) vs Median (23.0) ต่อค่าเฉลี่ย |
| **15** | **B** | Week 03 | ข้อผิดพลาด `ValueError: could not convert string to float: ''` |
| **16** | **B** | Week 03 | การคำนวณ Median ของ Pandas ข้าม NaN อัตโนมัติ: `30.0` |
| **17** | **B** | Week 03 | การรักษา `df` เดิมด้วย `df.assign()` ผลลัพธ์คือ `1` แล้วตามด้วย `0` |
| **18** | **B** | Week 03 | เหตุผลทางสถิติที่ต้องใช้ `sorted()` ก่อนคำนวณ Median ใน Pure Python |
| **19** | **B** | Week 04 | Operator Precedence: `&` ทำงานก่อน `<=` และ `>=` |
| **20** | **B** | Week 04 | `ValueError: The truth value of a Series is ambiguous` จาก keyword `and` |
| **21** | **B** | Week 04 | ตรรกะของ Bitwise OR (`|`): Mbappe, Haaland และ Pedri ผ่านทุกคน |
| **22** | **C** | Week 04 | ผลลัพธ์ของ `.isin()` คือ Series ของ Boolean (Boolean Mask) |
| **23** | **A** | Week 04 | ไวยากรณ์ข้อความใน `.query("position in ['ST', 'RW'] and physical >= 75")` |
| **24** | **B** | Week 04 | จำนวนคอลัมน์ของตารางหลังกรองและเลือกคอลัมน์ `sub.shape[1]` คือ `3` |
| **25** | **A** | Week 05 | โครงสร้างผลลัพธ์ของ `df.groupby()[]`: `Series` ที่มี Index ชื่อกลุ่ม |
| **26** | **B** | Week 05 | ชื่อคอลัมน์ของ DataFrame จาก `.agg(["count", "mean"])` คือ `['count', 'mean']` |
| **27** | **B** | Week 05 | ผลของ `.sort_values("mean", ascending=False)` |
| **28** | **B** | Week 05 | การวิเคราะห์ความน่าเชื่อถือของ Mean เมื่อ Sample size (count) เล็ก |
| **29** | **A** | Week 05 | การแก้ `KeyError: 'positions'` โดยเปลี่ยนเป็นเอกพจน์ `position` |
| **30** | **B** | Week 05 | ลำดับการประมวลผลของ Chained Pipeline ใน Week 05 |

---

## 📖 เฉลยละเอียดรายข้อ: ส่วนที่ 1 (ข้อ 1 - 30)

### Week 01: เริ่มต้นและรู้จักข้อมูล
1. **ตอบ B:** คำสั่ง `df.head(3)` จะหยิบเพียง 3 แถวแรกของข้อมูลออกมา โดยไม่ลดทอนจำนวนคอลัมน์ ตาราง `sample` จึงมี 3 แถว และ 15 คอลัมน์ ส่งผลให้ `sample.shape` คืนค่า Tuple เป็น `(3, 15)`
2. **ตอบ B:** Working Directory ของ Jupyter Notebook จะตั้งต้นที่ตำแหน่งโฟลเดอร์ที่บันทึกไฟล์นั้นอยู่ ซึ่งคือ `challenge/` การเรียก `data/footballers.csv` จะทำให้ระบบหาไฟล์ที่ `challenge/data/footballers.csv` ซึ่งไม่มีอยู่จริง จึงเกิด `FileNotFoundError` ทางแก้คือต้องใช้ `../` หนึ่งชั้น เพื่อถอยกลับไปที่โฟลเดอร์แม่ก่อน
3. **ตอบ B:** `csv.DictReader` เป็นโมดูลมาตรฐานของ Python ที่ไม่มีระบบตรวจจับชนิดข้อมูลอัตโนมัติ จึงอ่านค่าทุกอย่างเป็นข้อความ (`str`) เช่น `'90'` ในขณะที่ `pd.read_csv()` มีระบบ Type Inference ที่จะวิเคราะห์คอลัมน์ตัวเลขและแปลงให้เป็น `int64` โดยอัตโนมัติ
4. **ตอบ B:** `df.shape` เป็น Attribute ชนิด Tuple ในรูปแบบ `(แถว, คอลัมน์)` การเข้าถึงจำนวนแถวจึงต้องใช้การดึง Index ตัวแรกคือ `df.shape[0]` (การใส่ `()` จะทำให้เกิด `TypeError: 'tuple' object is not callable`)
5. **ตอบ B:** เมธอด `next(reader)` ของ `csv.DictReader` จะคืนค่าข้อมูลแถวนั้นออกมาในรูปของ Dictionary (`dict`) โดยมี Key เป็นชื่อคอลัมน์ และ Value เป็นค่าในแถวนั้น
6. **ตอบ B:** Dictionary ที่ส่งเข้าไปมี Key 2 ตัว (`list`, `price`) จึงกลายเป็น 2 คอลัมน์ และแต่ละ Key มีสมาชิก 3 ตัว จึงกลายเป็น 3 แถว มิติของตาราง `result.shape` จึงเป็น `(3, 2)`

### Week 02: Data Wrangling
7. **ตอบ A:** การดึงด้วยก้ามปูชั้นเดียว `df["name"]` จะได้ข้อมูลมิติเดียวคือ Pandas Series (จึงมี shape เป็น `(46,)`) ส่วนก้ามปูสองชั้น `df[["name"]]` จะได้ DataFrame ที่มี 46 แถว และ 1 คอลัมน์ ซึ่งมี shape เป็น `(46, 1)`
8. **ตอบ B:** เมธอด `.rename()` ใน Pandas ไม่ได้แก้ไขตารางเดิมในหน่วยความจำ (เว้นแต่จะระบุ `inplace=True` หรือกำหนดตัวแปรรับค่า) เมื่อไม่มีตัวแปรรับค่า ตาราง `df` จึงยังคงมีคอลัมน์ชื่อ `overall` เหมือนเดิม และไม่มีคอลัมน์ `rating`
9. **ตอบ B:** Python และ Pandas เป็น Case-Sensitive ชื่อคอลัมน์ในไฟล์จริงคือ `overall` (ตัวพิมพ์เล็ก) เมื่อระบุ `Overall` จึงหาคอลัมน์ไม่พบและเกิด `KeyError` ทันที
10. **ตอบ A:** ในบทเรียน Week 02 การ Wrangling ใน Pandas ที่เทียบเท่ากับการวนลูปสร้าง Dict คือการเลือกคอลัมน์ด้วยก้ามปูสองชั้น `df[["name", "club", "overall"]]` แล้วต่อด้วย `.rename(columns={"overall": "rating"})`
11. **ตอบ B:** `.copy()` ช่วยบังคับให้ Pandas สร้างสำเนาข้อมูลชุดใหม่ในหน่วยความจำ (Deep Copy) แยกขาดจาก `df` เดิม การเปลี่ยนแปลงใน `summary` จึงไม่ส่งผลกระทบย้อนกลับ และป้องกันการเกิด `SettingWithCopyWarning`
12. **ตอบ A:** `df.columns` คืออ็อบเจกต์ประเภท Index ของ Pandas (`pandas.core.indexes.base.Index`) เมื่อเรียก `.tolist()` จะแปลงเป็น `list` ธรรมดาของ Python

### Week 03: Cleaning Data
13. **ตอบ B:** ในไฟล์ `footballers.csv` แถวของ Casemiro (แถวที่ 20) ไม่มีข้อมูล `height_cm` และแถวของ Marquinhos (แถวที่ 30) ไม่มีข้อมูล `age` ดังนั้น `missing_age` และ `missing_height` จึงมีค่าเท่ากับ 1 จุดทั้งคู่
14. **ตอบ A:** การแทนด้วย 0 จะคำนวณ: `(20 + 22 + 24 + 26 + 0) / 5 = 92 / 5 = 18.4` ส่วนการแทนด้วย Median (23.0) จะคำนวณ: `(20 + 22 + 24 + 26 + 23) / 5 = 115 / 5 = 23.0` ตัวเลข 18.4 แสดงให้เห็นว่าเลข 0 ดึงค่าเฉลี่ยตกต่ำลงอย่างบิดเบือน
15. **ตอบ B:** เมื่อวนลูปมาถึงแถวที่มีสตริงว่าง `""` ฟังก์ชัน `float("")` ของ Python จะโยน Error ว่า `ValueError: could not convert string to float: ''` เพราะไม่สามารถแปลงข้อความว่างเปล่าเป็นตัวเลขได้
16. **ตอบ B:** ฟังก์ชันทางสถิติของ Pandas เช่น `.median()` หรือ `.mean()` จะข้ามค่า `NaN` ออกไปโดยอัตโนมัติ เหลือข้อมูล `[10, 20, 40, 50]` มัธยฐานคือค่าเฉลี่ยของ 20 และ 40 ซึ่งเท่ากับ `30.0`
17. **ตอบ B:** `df.assign()` คืนค่า DataFrame ตัวใหม่โดยไม่เปลี่ยนแปลง `df` เดิม ทำให้ `df["age"]` ยังคงมีค่าว่าง 1 จุด ส่วน `clean_df["age"]` มีค่าว่าง 0 จุด
18. **ตอบ B:** มัธยฐานค่านิยามคือค่ากึ่งกลางของข้อมูลที่เรียงลำดับขนาดแล้ว (Ranked data) หากไม่ใช้ `sorted()` ก่อน ค่าที่ได้จะเป็นเพียงตำแหน่งตรงกลางของไฟล์ดิบ ซึ่งไม่ใช่ค่ามัธยฐานจริง

### Week 04: Filter Data
19. **ตอบ B:** กฎลำดับการทำงาน (Operator Precedence) ของ Python กำหนดให้ตัวดำเนินการ Bitwise `&` มีลำดับสูงกว่าเครื่องหมายเปรียบเทียบ `<=` และ `>=` หากไม่ใส่วงเล็บ Python จะนำ `25 & df["overall"]` มาคำนวณก่อน ซึ่งทำให้การทำงานพังทันที จึงจำเป็นต้องครอบวงเล็บ `(...) & (...)`
20. **ตอบ B:** Keyword `and` ของ Python ต้องการค่าความจริงเพียง 1 ค่า (Scalar Boolean) แต่ Series มีค่า True/False หลายแถว ทำให้ Python ไม่สามารถตัดสินได้ (Ambiguous) จึงเกิด Error `ValueError: The truth value of a Series is ambiguous...`
21. **ตอบ B:** เครื่องหมาย `|` (Bitwise OR) หมายถึงขอแค่เข้าเงื่อนไขใดเงื่อนไขหนึ่งก็เป็นจริง:
    - Mbappe: overall = 91 (>= 90 เป็นจริง) -> ผ่าน
    - Pedri: age = 22 (<= 25 เป็นจริง) -> ผ่าน
    - Haaland: เข้าทั้งสองเงื่อนไข -> ผ่าน
    ดังนั้นผ่านการคัดกรองทั้ง 3 คน
22. **ตอบ C:** เมธอด `.isin()` ทำหน้าที่ตรวจสอบสมาชิกรายแถวและส่งคืนผลลัพธ์เป็น Series ของค่าความจริง (`True`/`False`) ประจำแต่ละแถว ซึ่งเรียกว่า Boolean Mask
23. **ตอบ A:** ใน String ของคำสั่ง `.query()` เราสามารถใช้คำสั่งภาษาพูด เช่น `in` และ `and` ได้โดยตรง โดยไม่ต้องใช้ `&` และไม่ต้องครอบวงเล็บซับซ้อน
24. **ตอบ B:** แม้ `df.query()` จะกรองแถว แต่โค้ดมีการระบุ `[["name", "club", "overall"]]` เพื่อเลือกเฉพาะ 3 คอลัมน์ที่ต้องการ ดังนั้น `sub.shape[1]` ซึ่งเป็นจำนวนคอลัมน์จึงมีค่าเท่ากับ 3

### Week 05: Analysis พื้นฐาน
25. **ตอบ A:** เมื่อนำ DataFrame มา `groupby("league")` แล้วเลือกคอลัมน์เดียว `["overall"]` มาหา `.mean()` ผลลัพธ์จะเป็น Pandas Series โดยมีชื่อของลีกกลายมาเป็น Index ของ Series นั้น
26. **ตอบ B:** เมื่อใช้ `.agg(["count", "mean"])` Pandas จะสร้างตารางใหม่ที่มีชื่อคอลัมน์ตามชื่อฟังก์ชันสถิติที่ระบุ คือ `'count'` และ `'mean'` โดยคงชื่อกลุ่ม (`nationality`) ไว้เป็น Index
27. **ตอบ B:** เมธอด `.sort_values("mean", ascending=False)` จะเรียงลำดับแถวจากค่าเฉลี่ยมากที่สุดไปหาน้อยที่สุด ทำให้แถวแรกสุด (`index[0]`) เป็นสัญชาติที่ได้คะแนนเฉลี่ยสูงสุด
28. **ตอบ B:** ค่าเฉลี่ยสูงของกลุ่มตัวอย่างที่มีเพียงคนเดียว (count = 1) ไม่อาจใช้เป็นตัวแทนประชากรทั้งประเทศได้ เพราะอาจเป็นเพียงความสามารถเฉพาะบุคคล (Outlier) การดู `count` ควบคู่กับ `mean` จึงเป็นหัวใจสำคัญของการวิเคราะห์ข้อมูลอย่างรับผิดชอบ
29. **ตอบ A:** เมื่อเกิด `KeyError` จากการสะกดชื่อคอลัมน์ผิด การแก้ไขที่ถูกต้องคือการแก้ชื่อในโค้ดให้ตรงกับคอลัมน์จริงในตารางคือ `"position"` (เอกพจน์)
30. **ตอบ B:** Pipeline ทำงานตามลำดับจากซ้ายไปขวา: 1. `groupby("league")` แบ่งกลุ่ม -> 2. `["overall"]` เลือกคอลัมน์ -> 3. `.mean()` คำนวณค่าเฉลี่ย -> 4. `.sort_values(ascending=False)` เรียงจากมากไปน้อย -> 5. `.round(1)` ปัดเศษทศนิยม 1 ตำแหน่ง

---

## 📝 เฉลยละเอียดและเกณฑ์การตรวจข้อสอบอัตนัย: ส่วนที่ 2 (5 ข้อ 20 คะแนน)

### ข้อเขียนที่ 1 (Week 01): การสำรวจมิติและตรวจจับ Relative Path (เต็ม 4 คะแนน)

#### แนวคำตอบที่สมบูรณ์ (Model Answer):
```python
import pandas as pd

# 1. โหลดไฟล์ CSV ด้วย Relative Path ที่ถูกต้องจากโฟลเดอร์ challenge/
df = pd.read_csv("../data/footballers.csv")

# 2. แสดงตัวอย่าง 3 แถวแรก
display(df.head(3))

# 3. แสดงจำนวนแถวและจำนวนคอลัมน์แยกจาก .shape
print("จำนวนแถว:", df.shape[0])
print("จำนวนคอลัมน์:", df.shape[1])
```
**คำอธิบายข้อ 4:**
- **ข้อผิดพลาดที่เกิดขึ้น:** `FileNotFoundError: [Errno 2] No such file or directory: 'data/footballers.csv'`
- **สาเหตุ:** ตำแหน่งโฟลเดอร์ปัจจุบัน (Working Directory) ของ Notebook อยู่ที่ `challenge/` การระบุ `data/footballers.csv` ทำให้ Python วิ่งไปค้นหาที่ `challenge/data/footballers.csv` ซึ่งไม่มีอยู่จริง
- **หลักการคิดในการนับระดับโฟลเดอร์:** เครื่องหมาย `../` หมายถึงการถอยกลับขึ้นไป 1 ระดับจากโฟลเดอร์ปัจจุบัน เพื่อกลับไปที่โฟลเดอร์หลัก (`data-sci/`) จากนั้นจึงระบุทางเข้าสู่โฟลเดอร์ข้อมูลเป็น `../data/footballers.csv`

#### เกณฑ์การให้คะแนน (Scoring Rubric):
- **1.0 คะแนน:** เขียนคำสั่ง `pd.read_csv("../data/footballers.csv")` ถูกต้องตาม Relative Path
- **1.0 คะแนน:** ใช้คำสั่ง `df.head(3)` หรือ `display(df.head(3))` แสดง 3 แถวแรกได้ถูกต้อง
- **1.0 คะแนน:** ดึงจำนวนแถวและคอลัมน์จาก `df.shape[0]` และ `df.shape[1]` ถูกต้อง (ไม่ใส่วงเล็บที่ `.shape`)
- **1.0 คะแนน:** อธิบายชื่อ Error (`FileNotFoundError`), สาเหตุจาก Working Directory ใน Subfolder และหลักการใช้ `../` ได้อย่างถูกต้องชัดเจน

---

### ข้อเขียนที่ 2 (Week 02): Data Wrangling, Immutability & Case Sensitivity (เต็ม 4 คะแนน)

#### แนวคำตอบที่สมบูรณ์ (Model Answer):
```python
# 1. คัดเลือก 3 คอลัมน์และสร้างตารางอิสระด้วย .copy() พร้อมเปลี่ยนชื่อ
summary = df[["name", "club", "overall"]].copy()
summary = summary.rename(columns={"overall": "rating"})

# 2. ตรวจสอบยืนยันว่า df เดิมไม่ถูกเปลี่ยนแปลง
print("overall in df.columns:", "overall" in df.columns)   # แสดงผล True
print("rating in df.columns:", "rating" in df.columns)     # แสดงผล False
print("rating in summary.columns:", "rating" in summary.columns) # แสดงผล True
```

**คำอธิบายข้อ 3:**
- **ทำไม `df["name"]` ได้ Series แต่ `df[["name"]]` ได้ DataFrame:**
  การใช้ก้ามปูชั้นเดียว `df["name"]` เป็นการส่ง String ชื่อคอลัมน์เข้าไปตัวเดียว Pandas จะดึงข้อมูลออกมาในรูปของ 1D Series แต่การใช้ก้ามปูสองชั้น `df[["name"]]` เป็นการส่ง List ของคอลัมน์เข้าไป ทำให้ Pandas คืนค่ากลับมาในรูปของตาราง 2D DataFrame เสมอ
- **ทำไมการใช้ `.copy()` จึงจำเป็นในการป้องกัน SettingWithCopyWarning:**
  หากไม่ใส่ `.copy()` ตารางใหม่ที่ได้อาจถูกมองว่าเป็นเพียง "View" หรือภาพสะท้อนที่ยังคงอ้างอิงหน่วยความจำร่วมกับตาราง `df` ต้นฉบับ เมื่อมีการแก้ไขหรือเปลี่ยนค่าในตารางใหม่ Pandas จะแจ้งเตือน `SettingWithCopyWarning` เพื่อเตือนว่าการกระทำนี้อาจไปกระทบหรือไม่ได้ตั้งใจเปลี่ยนค่าใน `df` เดิม การใส่ `.copy()` จึงเป็นการบังคับสร้าง Deep Copy แยกหน่วยความจำออกมาอย่างชัดเจนและปลอดภัย

#### เกณฑ์การให้คะแนน (Scoring Rubric):
- **1.0 คะแนน:** ใช้ก้ามปูสองชั้น `[["name", "club", "overall"]]` และ `.copy()` ได้ถูกต้อง
- **1.0 คะแนน:** ใช้ `.rename(columns={"overall": "rating"})` เปลี่ยนชื่อคอลัมน์ได้ถูกต้อง
- **1.0 คะแนน:** เขียนโค้ดตรวจสอบยืนยันผลว่า `overall in df.columns` เป็น True และ `rating` ไม่อยู่ใน `df`
- **1.0 คะแนน:** อธิบายความแตกต่างของมิติระหว่างก้ามปู 1 ชั้น vs 2 ชั้น และอธิบายสาเหตุการป้องกัน SettingWithCopyWarning ด้วย `.copy()` ได้อย่างถูกต้องสมบูรณ์

---

### ข้อเขียนที่ 3 (Week 03): Data Cleaning, Missing Values & Statistical Defense (เต็ม 4 คะแนน)

#### แนวคำตอบที่สมบูรณ์ (Model Answer):
```python
# 1. ตรวจนับ Missing Values ของ age และ height_cm
print("Missing age:", df["age"].isna().sum())
print("Missing height_cm:", df["height_cm"].isna().sum())

# 2. คำนวณ Median และแทนค่าสร้าง clean_df อย่างปลอดภัย
med_height = df["height_cm"].median()
clean_df = df.assign(height_cm=df["height_cm"].fillna(med_height))

# ตรวจสอบยืนยัน
print("Missing height_cm in clean_df:", clean_df["height_cm"].isna().sum()) # ได้ 0
print("Missing height_cm in df:", df["height_cm"].isna().sum())             # ได้ 1
```

**คำอธิบายข้อ 3:**
- **ผลกระทบจากการแทนส่วนสูงด้วยเลข `0`:**
  ส่วนสูง 0 ซม. ไม่มีจริงในหมู่นักฟุตบอลอาชีพ เลข 0 จะกลายเป็นค่าต่ำสุดโต่งที่ดึงผลรวมลงมาอย่างรุนแรง ทำให้ค่าเฉลี่ยส่วนสูงรวม (`mean`) ของนักฟุตบอลทั้งตารางลดลงฮวบอย่างผิดธรรมชาติและบิดเบือนข้อเท็จจริง
- **เหตุผลที่ควรใช้ Median แทน Mean เมื่อมี Outliers:**
  มัธยฐาน (Median) คือค่าที่อยู่ตรงกึ่งกลางของข้อมูลหลังจากนำมาเรียงลำดับขนาด จึงมีความทนทานต่อค่าผิดปกติสุดโต่ง (Robust to Outliers) ไม่ว่าในชุดข้อมูลจะมีนักเตะที่ตัวสูงผิดปกติ (เช่น 220 ซม.) หรือเตี้ยผิดปกติ ค่ามัธยฐานจะไม่ถูกดึงขยับตาม แตกต่างจากค่าเฉลี่ย (Mean) ที่นำทุกค่ามารวมกันแล้วหารด้วยจำนวนคน ทำให้ค่าเฉลี่ยอ่อนไหวและบิดเบือนได้ง่ายมากเมื่อเจอ Outlier

#### เกณฑ์การให้คะแนน (Scoring Rubric):
- **1.0 คะแนน:** ตรวจสอบและนับค่าว่างด้วย `.isna().sum()` ทั้งสองคอลัมน์ได้ถูกต้อง
- **1.0 คะแนน:** คำนวณ Median ของส่วนสูง และใช้ `df.assign(...)` ร่วมกับ `.fillna()` ได้อย่างถูกต้อง
- **1.0 คะแนน:** แสดงผลยืนยันว่า `clean_df` ค่าว่างหมดไป และ `df` เดิมไม่ถูกแก้ไข
- **1.0 คะแนน:** ให้เหตุผลทางสถิติเรื่องผลกระทบของการแทนด้วย 0 และเปรียบเทียบความทนทานต่อ Outliers ของ Median เหนือกว่า Mean ได้อย่างชัดเจน

---

### ข้อเขียนที่ 4 (Week 04): Complex Filtering, Precedence & Query (เต็ม 4 คะแนน)

#### แนวคำตอบที่สมบูรณ์ (Model Answer):
```python
# 1. คำอธิบายสาเหตุและ Precedence:
# - สาเหตุ: Keyword 'and' ของ Python ใช้เปรียบเทียบค่าความจริงเดี่ยว (Single Boolean True/False) แต่เงื่อนไขใน Pandas คืนค่าเป็น Series ที่มี True/False หลายแถว Python จึงตัดสินไม่ได้ (ambiguous)
# - ทำไมต้องใส่วงเล็บ: ตัวดำเนินการระดับบิต '&' มีลำดับความสำคัญ (Operator Precedence) สูงกว่าเครื่องหมายเปรียบเทียบ (<=, >=) หากไม่ใส่วงเล็บ Python จะนำตัวเลขไปทำ Bitwise AND กับ Series ถัดไปก่อน ทำให้ลำดับผิดพลาดและเกิด Error ทันที

# 2. โค้ดแก้ไขด้วย Boolean Masking (& และวงเล็บ):
cond = (df["age"] <= 25) & (df["position"].isin(["ST", "RW"])) & (df["overall"] >= 85)
result1 = df[cond][["name", "age", "position", "overall"]]
display(result1)

# 3. โค้ดทางเลือกด้วย .query():
result2 = df.query("age <= 25 and position in ['ST', 'RW'] and overall >= 85")[["name", "age", "position", "overall"]]
display(result2)
```

#### เกณฑ์การให้คะแนน (Scoring Rubric):
- **1.0 คะแนน:** อธิบายสาเหตุของ Error จาก `and` กับ Series และอธิบายเรื่อง Operator Precedence ของ `&` ได้ถูกต้อง
- **1.5 คะแนน:** แก้ไขด้วย Boolean Masking โดยใช้ `&` และใส่วงเล็บครอบแต่ละเงื่อนไขอย่างถูกต้องสมบูรณ์ พร้อมเลือกเฉพาะ 4 คอลัมน์ที่กำหนด
- **1.5 คะแนน:** เขียนคำสั่ง `.query("age <= 25 and position in ['ST', 'RW'] and overall >= 85")` ได้อย่างถูกต้อง

---

### ข้อเขียนที่ 5 (Week 05): Advanced Groupby Aggregation & Responsible Reporting (เต็ม 4 คะแนน)

#### แนวคำตอบที่สมบูรณ์ (Model Answer):
```python
# 1, 2, 3. จัดกลุ่ม league คำนวณ count & mean ของ overall เรียงจากมากไปน้อย ปัดเศษ 1 ตำแหน่ง
league_stats = (
    df.groupby("league")["overall"]
    .agg(["count", "mean"])
    .sort_values("mean", ascending=False)
    .round(1)
)
display(league_stats)
```

**บทวิเคราะห์และข้อความสรุปผลอย่างมีความรับผิดชอบ (Responsible Reporting):**
> *"ในชุดข้อมูลนี้ ลีกที่มีค่าคะแนน overall เฉลี่ยสูงที่สุดคือลีก MLS (เฉลี่ย 90.0) อย่างไรก็ตาม เมื่อพิจารณาขนาดกลุ่มตัวอย่างพบว่าลีก MLS มีข้อมูลนักเตะเพียง 1 คนเท่านั้น (Lionel Messi) ซึ่งถือเป็นกับดักทางสถิติที่ค่าเฉลี่ยสะท้อนเพียงความสามารถของยอดนักเตะรายบุคคล ไม่ได้สะท้อนมาตรฐานของนักเตะทั้งลีก MLS แต่อย่างใด ในขณะที่ Premier League มีตัวอย่างถึง 15 คนซึ่งมีความน่าเชื่อถือทางสถิติมากกว่า ดังนั้นจึงสรุปได้เพียงว่า 'ในกลุ่มตัวอย่าง 46 คนนี้ นักเตะจาก MLS มีคะแนนสูงสุด' แต่ไม่สามารถสรุปเหมารวมว่าลีก MLS เหนือกว่าลีกอื่นได้เนื่องจากข้อจำกัดด้านขนาดกลุ่มตัวอย่าง"*

#### เกณฑ์การให้คะแนน (Scoring Rubric):
- **1.0 คะแนน:** ใช้ `df.groupby("league")["overall"]` ได้ถูกต้อง
- **1.0 คะแนน:** ใช้ `.agg(["count", "mean"])` และ `.sort_values("mean", ascending=False)` พร้อม `.round(1)`
- **1.0 คะแนน:** เขียนบทวิเคราะห์ชี้ให้เห็นกับดักทางสถิติของขนาดกลุ่มตัวอย่าง (Count = 1 vs Count = 15)
- **1.0 คะแนน:** สรุปผลโดยมีขอบเขต "ในชุดข้อมูลนี้" อ้างหลักฐานตัวเลข และระบุข้อจำกัดของข้อมูลอย่างมีความรับผิดชอบ
